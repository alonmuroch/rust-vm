[package]
name = "examples"
version = "0.1.0"
edition = "2024"

[dependencies]
compiler = { path = "../compiler" }
program = { path = "../program" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[[bin]]
name = "simple"
path = "simple.rs"

[[bin]]
name = "storage"
path = "storage.rs"
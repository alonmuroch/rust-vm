[package]
name = "program"
version = "0.1.0"
edition = "2024"

[lib]
test = false

[dependencies]
hex = { version = "0.4", default-features = false }
heapless = { version = "0.8", default-features = false }
